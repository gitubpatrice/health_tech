# Sécurité — Health Tech

## Modèle de menace

**Données protégées** : identité client, antécédents santé, comptes rendus de séance, photos d'animaux, documents joints (ordonnances, consentements).

**Adversaires considérés** :

1. **Téléphone perdu / volé, écran verrouillé** — protection : Keystore Android, Argon2id avec coût mémoire 64 MiB, FLAG_SECURE.
2. **Téléphone volé déverrouillé** — protection : verrouillage applicatif distinct (phrase secrète), auto-lock après inactivité.
3. **Application malveillante sur l'appareil** — protection : `allowBackup="false"`, `data_extraction_rules` exclut tout, pas d'export Intent permissif.
4. **Sauvegarde Android compromise** — protection : sauvegarde désactivée explicitement.
5. **Dump RAM via root** — protection partielle : VEK conservée le moins longtemps possible, wipe sur `lock()` et au niveau `FieldCrypto.dispose()`.

**Hors scope** : adversaire avec accès root persistant et patch du processus en mémoire.

## Architecture cryptographique

```
┌─────────────────────────┐
│  Phrase secrète user    │
└──────────┬──────────────┘
           │ Argon2id (mem=64MiB, t=3, p=1)
           ▼
   ┌──────────────────┐
   │   Master Key 32B │  (jamais persistée)
   └────────┬─────────┘
            │ AES-256-GCM (wrap)
            ▼
   ┌──────────────────┐
   │ Vault Encrypt Key│  (random 32B, persistée wrappée)
   └────────┬─────────┘
            │ AES-256-GCM (per field)
            ▼
   ┌──────────────────┐
   │ Champs sensibles │  (santé, comptes rendus, notes)
   └──────────────────┘

   ┌──────────────────┐
   │ SQLCipher (db)   │  (clé = hex(VEK))
   └──────────────────┘
```

## Décisions de conception

- **Deux clés (Master + VEK)** : permet le changement de phrase secrète sans réécrire les ciphertexts. Seule la clé wrappée change.
- **Chiffrement par champ AES-GCM** en plus de SQLCipher : si la base fuit en clair (cas extrême : dump après ouverture), les champs sensibles restent illisibles.
- **FTS5 indexe uniquement les métadonnées non sensibles** (nom, prénom, email, téléphone). Les comptes rendus et notes santé ne sont JAMAIS indexés FTS — sinon le contenu fuirait via les structures internes FTS.
- **`allowBackup=false`** : aucune copie cloud automatique.
- **FLAG_SECURE** : actif dès `onCreate` de MainActivity (pas seulement après le chargement de Flutter).

## Bonnes pratiques côté code

- Toute donnée sensible chiffrée au niveau champ DOIT passer par `FieldCrypto`.
- Aucune écriture en clair sur disque persistant. Cache export uniquement dans `cache/` (purgé au boot et sur `paused`).
- Atomic write systématique pour les fichiers (réutiliser `files_tech_core/atomic_write`).
- Lints stricts (`avoid_print`, `strict-casts`, `strict-inference`).

## Dépendances critiques

| Paquet | Rôle | Audité |
|---|---|---|
| `sqlcipher_flutter_libs` | Chiffrement DB | upstream Zetetic |
| `cryptography` | AES-GCM, Argon2id | dart team / community |
| `flutter_secure_storage` | Persistance clé wrappée | community, EncryptedSharedPreferences |
| `local_auth` | Biométrie | flutter team |

## Historique des versions

| Version | Date | Notes principales |
|---|---|---|
| **v1.8.1** | 2026-05-20 | **Audit final post-v1.8.0 — patch UX cohérence**. HIGH-1 : `AnimalDetailScreen` tab Sessions — tap pousse maintenant `SessionDetailScreen` sur compact (téléphone) ; avant : tile inerte. HIGH-2 : `GlobalSearchScreen._open(SearchHit)` capture le Navigator AVANT pop puis push `ClientDetailScreen` / `AnimalDetailScreen` / `SessionDetailScreen` sur compact ; avant : utilisateur retombait sur la liste sélectionnée sans aucune fiche détail. MEDIUM 1-4 : `autofocus: true` ajouté sur les boutons Cancel des 4 dialogs destructifs restants (suppression template, purge RGPD client, restauration backup, confirmation panique) — alignement complet avec le pattern `confirm_delete_dialog` v1.6.0 U10. Écran « À propos » : retrait du numéro SIRET (l10n FR + EN), ajout d'une rangée `aboutWebsiteLabel/Value` (https://files-tech.com avec bouton `open_in_new`), ajout du bouton « Vérifier les mises à jour » qui ouvre `https://github.com/gitubpatrice/health_tech/releases/latest` via `url_launcher`. `riverpod_annotation` déplacé de `dependencies` vers `dev_dependencies` (codegen build-time only, économie marginale APK). flutter analyze 0, 133/133 tests verts. Pas de changement crypto / DB / vault / `.htbk`. |
| **v1.8.0** | 2026-05-20 | **Audit cohérence C6 — Migration Riverpod v1 → v2** : `VaultSessionController` + `AutoLockController` passent de `StateNotifier` à `Notifier`, providers passent à `NotifierProvider`. Le `_ref` boilerplate disparaît (ref hérité), `dispose()` remplacé par `ref.onDispose(...)` déclaratif dans `build()`. API call-side identique → 6 fichiers consommateurs intouchés. **2 bugfixes UX critiques** : `ClientDetailScreen` tabs Animals + Sessions — tap pousse désormais l'écran détail correspondant sur compact (téléphone), avant : `selectedXxxIdProvider.notifier.state` était set mais aucun écran ne s'affichait. Pattern aligné `animals_screen._select` (context.isCompact → MaterialPageRoute push). 6 nouveaux tests garde-régression dans `audit_v1_8_0_test.dart` verrouillent le shape `NotifierProvider<C,T>` (régression vers StateNotifierProvider casse compile). 133/133 verts. Pas de changement crypto / DB / vault. |
| **v1.7.2** | 2026-05-20 | **Audit cohérence post-v1.7.1**. (M5) 41 `SnackBar` raw migrés vers `showFloatingSnack` / `buildFloatingSnack(scheme)` (backup_screen 6, settings_screen 2, session_detail 1, attachments_section 2, animal_detail 1, about_screen 1, appointment_form 5, session_form 5, busy_helpers.showErrorSnack). Tonalités Material 3 info/success/error cohérentes WCAG AA. Post-await sites capturent `scheme` pré-await pour respecter `use_build_context_synchronously`. (M3) `attachmentBytesProvider` keepAlive 30s : `ref.keepAlive() + Timer(30s, link.close) + ref.onDispose(timer.cancel)` — évite re-decrypt à chaque transition liste↔détail (navigation rapide), libéré au verrouillage vault. 127/127 verts. Pas de changement crypto / DB / vault. |
| **v1.7.1** | 2026-05-20 | **Audit expert 3 axes + cohérence sur 35 605 lignes Dart** (app la plus grosse du portfolio Files Tech). 0 Critical, 9 fixes appliqués (4 HIGH + 3 MEDIUM + C4 tests + M6 analyze). Sécurité : (C3 HIGH RGPD) `PanicService.systemCalendarWipe` ajouté avant `vaultDestroy`, lit `externalCalendarId` + `externalCalendarEventId` des appointments + sessions et appelle `SystemCalendarBridge.remove` par item — avant : rendez-vous post-panic lisibles dans Calendar Android par toute app avec `READ_CALENDAR`. (C2 HIGH cohérence) `HealthVault._lockoutStopwatch` monotone en complément du timestamp wall-clock, read prend `max(wallRemaining, monoRemaining)` — attaquant root qui AVANCE clock OS ne peut plus annuler le backoff. (H1) `setAvatar` refuse explicitement `image/gif` (`AttachmentRejectedError('image_format_unsupported')`) — GIF animé 20 Mo passait cap 25 Mo et OOM au rendu MemoryImage. Build : (H3) `isUniversalApk=false` portfolio-aligned gain ~30 Mo. UI/a11y : `showFloatingSnack` sur `animal_form._save` clientId null, `confirm_delete_dialog` `autofocus: true` sur Cancel pattern Files Tech, `HapticFeedback.heavyImpact()` avant panic.wipe + `mediumImpact()` au confirm delete — premier usage haptique du projet. Tests : nouveau `audit_v1_7_1_test.dart` (7 garde-régression PanicStep order + AttachmentRejectedError). Suite 127/127 verts. flutter analyze 0. |
| **v1.7.0** | 2026-05-14 | **Photo-avatar de fiche client / animal**. Nouveau widget `AvatarPicker` (capture caméra + galerie + suppression) et `OwnerAvatar` (vignette circulaire lecture seule, tap → viewer plein écran). API repo : `setAvatar` / `getAvatar` / `watchAvatar` / `clearAvatar` au-dessus du pipeline `importBytes` existant — **aucune nouvelle surface crypto**. Invariant « au plus un avatar `deletedAt IS NULL` par couple `(ownerType, ownerId)` » garanti applicativement (purge-puis-importe). Sécurité : (a) photo chiffrée AES-GCM via `FieldCrypto` dans `<appSupport>/attachments/<uuid>.enc` comme tout attachment ; (b) `requestFullMetadata: false` côté `image_picker` strip EXIF / GPS dès la lecture (pas de fuite des coordonnées du cabinet) ; (c) `ImageBoundsProbe` (anti-bombe PNG/JPEG/WebP/GIF) déclenché AVANT decode ; (d) `ImageCompress.maybeCompress` en isolate (downscale 2048 px JPEG q90) ; (e) garde-fou amont `setAvatar` refuse les `mimeType` non-`image/*` — un PDF passé par erreur retombe sur `AttachmentRejectedError` ; (f) cache `image_picker` (`<temp>/image_picker/`) purgé après chaque action OK ou erreur ; (g) `PaintingBinding.imageCache` clear au `vault.lock()` — bitmap déchiffré wipé en RAM ; (h) `PanicService` wipe récursif `<appSupport>/attachments/` (avatars inclus) ; (i) `PurgeService.purgeClient/purgeAnimal` cascade `purgeAllForOwner` (cascade avatar testée) ; (j) `RgpdExportService` passe explicitement `excludeKinds: const {}` → l'avatar est inclus dans l'export Article 15 (donnée personnelle du sujet). UX : (a) `Semantics(label: …, button: true)` au top-level du picker ; (b) BottomSheet Material 3 avec `showDragHandle: true` ; (c) pictogramme camera bottom-right (signal visuel "éditable") ; (d) overlay progress lors d'une opération ; (e) `showFloatingSnack` Files Tech standard pour feedback ; (f) dialog confirmation `autofocus: true` sur Cancel + `cs.error` sur Delete (pattern v1.6.0 U10) ; (g) tap zone full circle 96 dp (largement au-dessus WCAG 24 dp). Architecture : (a) `AttachmentKind.avatar` volontairement HORS de `AttachmentKind.all` (séparation d'écran : la liste générique des pièces jointes ne montre PAS l'avatar) ; (b) `AttachmentRepository.watchByOwner` accepte un nouveau paramètre `excludeKinds` avec default `{avatar}` — backward-compatible, aucun consumer cassé ; (c) `AvatarPickerController` value-holder pur pour le mode différé (création d'entité — `ownerId` pas encore connu) ; (d) providers `ownerAvatarProvider` (`StreamProvider.autoDispose.family`) + `attachmentBytesProvider` (`FutureProvider.autoDispose.family`) — pas d'accumulation de subscriptions au scroll, decrypt mutualisé entre liste + détail. Perf : `OwnerAvatar` cap `cacheSize = radius × 2 × dpr × 2` via `ResizeImage` — bitmap décodé borné (vignette 40 dp ≈ 80 Ko décodés au lieu de plusieurs Mo). Permissions Android : `CAMERA` + `<uses-feature required="false"> × 2` (visibilité Play Store sur tablettes maintenue). Nouvelle dépendance : `image_picker: ^1.1.2`. Tests : 11 nouveaux (`attachment_avatar_test.dart`), 120/120 verts, `flutter analyze` 0 issue. **Aucun changement de schéma DB** : l'avatar utilise la table `attachments` existante. |
| **v1.6.1** | 2026-05-13 | **Audit expert post-v1.6.0 — toutes les corrections livrées**. Sécurité : (F1) tap téléphone/email vétérinaire ouvre désormais `tel:` / `mailto:` via `url_launcher` au lieu de `Clipboard.setData` (les listeners clavier Android 12+ historisaient les coordonnées). (F2) Export RGPD Article 15 sort `emergency_contact` dans un fichier dédié `third_party_contacts.json` avec note explicite « PII tierce non-signataire » — le DPO / régulateur reçoit un signal clair que ce contact mérite un traitement séparé. (F7+U1) `_emergencyName` / `_emergencyPhone` passent par `SensitiveTextField` (étendu pour accepter `keyboardType` + `textCapitalization`) — fin du cloud-autocomplete pour les PII tierces. (F4+P1) Le seed des 6 templates système tourne maintenant dans une transaction Drift unique avec re-check `hasAnySystemTemplate()` à l'intérieur — neutralise la race au double-unlock et passe de 12 round-trips SQLCipher à 1 fsync WAL. (C8/G2) `seedDefaultsIfEmpty` reçoit la `Locale` système (`PlatformDispatcher.instance.locale`) au lieu de `Locale('fr')` codé en dur. (F6) `PRAGMA wal_checkpoint(TRUNCATE)` après `delete` template — aligne l'observable au claim « irréversible ». (F8) Whitelist `Lifestyle.*Values.contains(v)` + `ContactSource.all.contains(v)` au read de `profile_json` — valeur arbitraire injectée via `.htbk` forgé retombe à `null` au lieu de polluer le state. (F9) `vetEmail` validé via `HealthValidators.optionalEmail`, `vetClinic` / `vetPhone` / `vetName` cappés à 120/32/120 chars + strip-RTL au save. (F10) Nom de template cappé à 80 + strip caractères de contrôle bidirectionnel (U+200B-U+200F, U+202A-U+202E, U+2066-U+2069, U+FEFF — anti-phishing PDF). (F5) Commentaire-contrat explicite dans `database.dart::beforeOpen` (« que des PRAGMA »). UI / a11y : (U2) `showFloatingSnack(SnackTone.error)` standardisé pour le snack « consentements requis » du formulaire client. (U3) Tooltip du `PopupMenuButton` kebab passé de `actionEdit` à nouvelle clé `actionMore` (« Plus d'actions »). (U4) `BottomSheet` du picker en `DraggableScrollableSheet` — la sélection de modèle s'étend jusqu'à 90 % de hauteur sur petit téléphone avec 20+ canevas. (U5) Sections optionnelles du formulaire client (source / urgence / hygiène de vie) passées en `ExpansionTile` repliables avec auto-expand si au moins un champ est renseigné — fin de la scroll-fatigue 16 sections. (U6) Helper text ajouté au formulaire de template (« Pré-remplis seulement les sections utiles… ») + `maxLines: 6` pour les 9 canevas. (U7) `Semantics(label: …)` enrichi sur le badge « Système » pour les lecteurs d'écran. (U10) Mention « À renouveler le … » accolée à `Icons.warning_amber_outlined` — a11y daltonien. Perf : (P2+P3) `allReportTemplatesProvider` et `reportTemplatesByKindProvider` passés en `.autoDispose` — fin des subscriptions Drift orphelines après fermeture BottomSheet / écran Réglages. (P4) `_HomeShellState._resolveDestinations` mémoize la liste des 6 destinations par `Locale` — plus d'allocation à chaque changement d'onglet. (P5) Messenger capturé avant l'`await launchUrl` côté `_VetCard._launch`. Qualité : (C6) Nouveau `lib/utils/validators.dart` (`HealthValidators.optionalEmail` / `cleanShortLabel` / `stripBidiOverrides` / `maxLength`) — fin de `_emailRe` privé dupliqué entre formulaires. (C7) Test exhaustivité `lifestyleLabel` / `contactSourceLabel` / `reportTemplateKindLabel` × FR + EN — un ajout futur à `Lifestyle.dietValues` sans mapping l10n fait casser le test au CI. (G1+F13) Test de garde strict `BackupService.maxSupportedDbUserVersionForTesting == HealthDb.forTesting().schemaVersion` — l'oubli du bump symétrique au prochain `schemaVersion` ne pourra plus passer en release (régression historique v1.5.0 → v1.5.4). Nouvelle dépendance : `url_launcher: ^6.3.1`. 109/109 tests verts (+15 nouveaux), `flutter analyze` 0 issue. |
| **v1.6.0** | 2026-05-13 | **Modèles de comptes rendus + complétion fiches client/animal** (6 fonctionnalités spec verrouillée). (a) **DB v6** : nouvelle table `report_templates` (`id`, `name`, `kind`, `sections_json`, `is_system`, timestamps) — aucune migration de données sur les tables existantes (les champs cosmétiques passent par les JSON déjà en place `clients.profile_json` et `animals.identifiers_json` : rétro-compatibilité totale). `BackupService._maxSupportedDbUserVersion` bumpé 5→6 (audit C1 v1.5.4 — un `.htbk` v1.6.x doit rester restaurable). (b) **Seed idempotent** de 6 templates système au 1er unlock post-upgrade (« Première séance Reiki — humain », « Séance de suivi — humain », « Soin énergétique — animal », « Accompagnement fin de vie », « Soin à distance », « Bilan énergétique complet ») — l'utilisateur peut supprimer / dupliquer / modifier ; pas de ré-injection si la table contient déjà ≥ 1 row `is_system = 1`. (c) **Réglages → Modèles de comptes rendus** : liste filtrable, badge « Système », menu kebab (édit / dupliquer / supprimer avec hint « ne sera pas ré-injecté »), formulaire 9 champs aligné `SessionReport`. (d) **Session form** : bouton `OutlinedButton.icon` « Insérer un modèle » au-dessus du compte rendu, `BottomSheet` filtré par `SessionKind` (les templates `other` / `distance` toujours inclus), confirmation avant remplacement si le rapport contient déjà du texte. (e) **Client form (particulier)** : 3 nouvelles sections — « Source du contact » (dropdown 7 options + non renseigné), « Contact d'urgence » (nom + téléphone optionnels, bloc persisté ou retiré entièrement), « Hygiène de vie » (5 dropdowns optionnels : tabac, sport, sommeil, stress, alimentation). (f) **Client detail** : DetailRow source + urgence si renseignés, Card « Hygiène de vie » si au moins un axe renseigné. (g) **Animal form / detail** : 4e champ `vetClinic` (cabinet), section « Vaccination » dédiée (dernier vaccin + prochain rappel via date pickers + notes chiffrées via `SensitiveTextField`), Card « Vétérinaire » (tap téléphone / email = copie presse-papiers + snack confirmation), Card « Vaccination » avec mention `cs.error` « À renouveler le … » si `nextVaccinationAt` passé. (h) **Tests** : 19 nouveaux tests (round-trip Drift, idempotence du seed, ordre system-first, mapping `toSessionReport`, helpers `ClientProfileExt`, rétro-compat `AnimalIdentifiers.fromJson` sur ancien JSON). 94/94 verts, `flutter analyze` 0 issue. (i) Aucune nouvelle dépendance — tap-to-call utilise `Clipboard.setData` aligné `about_screen`. |
| **v1.5.6** | 2026-05-13 | UX feedback Réglages biométrie. `_toggle` du `_BiometricTile` discrimine désormais trois cas distincts via `BiometricFailure` typé : (a) succès → snack `floating` cs.surface « Biométrie activée / désactivée » ; (b) annulation utilisateur (`userCancelled`) → snack `floating` cs.errorContainer « Activation biométrique annulée. » sans bruit alarmiste ; (c) `keyInvalidated` → message clair « Empreinte Android modifiée… » alignement avec le LockScreen v1.5.5 ; (d) autre échec → « Activation biométrique échouée. ». Avant : tout passait par un `catch on Object` générique qui rendait l'annulation indistinguable d'un vrai échec. Aligne le pattern de feedback avec Pass Tech v2.4.2. |
| **v1.5.5** | 2026-05-13 | **Hotfix biométrie post-réenrôlement**. Repro : utilisateur supprime puis ré-enrôle son empreinte Android → ouvre Health Tech → bouton « Déverrouiller avec la biométrie » reste affiché mais tap = aucune réaction. Cause : la clé Keystore est invalidée par `setInvalidatedByBiometricEnrollment(true)` ; le 1er essai (auto-prompt) propage `BiometricFailure(key_invalidated)`, le vault appelle `disableBiometric()` qui efface IV/CT côté secure storage, mais le `biometricStatusProvider` (FutureProvider cache) garde son ancienne valeur `enrolled: true` → le bouton continue à s'afficher → au tap, `unlockWithBiometric` voit IV/CT absents → throw `VaultNotInitialisedError` → **avalé silencieusement** par un `on Object` générique. Fix : `_tryBiometric` discrimine maintenant `BiometricFailure(keyInvalidated)`, `VaultNotInitialisedError` et autres, affiche un message localisé clair (« Empreinte Android modifiée : biométrie désactivée par sécurité. Déverrouillez avec votre phrase secrète, puis réactivez la biométrie dans Réglages. »), et **invalide `biometricStatusProvider` en `finally`** pour que le bouton disparaisse dès le rebuild suivant — plus de tap muet. |
| **v1.5.4** | 2026-05-13 | **Hotfix critique + nettoyage post-audit**. (C1, bloqueur silencieux) `BackupService._maxSupportedDbUserVersion` était resté à 4 alors que `HealthDb.schemaVersion` est passé à 5 en v1.5.0 (`filename_encrypted`) — toute `.htbk` produite par v1.5.x **n'était pas restaurable par son propre créateur** (`backup_db_version_too_new`). Aligné à 5 ; commentaire de garde croisé entre les deux fichiers. (H1) Dialog passphrase backup converti en `StatefulWidget` pour disposer ses deux `TextEditingController` — auparavant un controller fuyait à chaque ouverture du dialog. (H4) Provider doublon `upcomingAppointmentsProvider` côté agenda renommé en `agendaUpcomingProvider` — supprime la double-subscription Drift sur la même requête. (H5) `_DebouncedSearchField` extrait dans `lib/widgets/debounced_search_field.dart` et adopté par l'écran animaux (avant : requête Drift LIKE à chaque keystroke, désormais 250 ms debounce). (H6) `AttachmentTooLargeError` et `AttachmentRejectedError` migrés en sous-classes `HealthError` (sealed) ; `attachments_section` discrimine désormais `image_too_large` (anti-bombe) de `image_format_unrecognised` avec deux messages distincts. (M5) Migration des 5 derniers `Center(Text(...))` ad-hoc vers le widget partagé `EmptyState`. (M6) Onglet sessions d'un animal : tile désormais tappable (sélection cohérente avec l'onglet sessions d'un client). (M7) Doublons `setState(_busy=true)` retirés des formulaires animaux et clients (`runWithBusy` gère seul). (M10) `homeStatsProvider` propage la **vraie** `stackTrace` d'origine au lieu d'un `StackTrace.current` capturé à l'endroit du test — crash reports désormais utiles. (M2/M16/M17) Suppressions de surface morte : `AdaptiveScaffold.floatingActionButton` (param orphelin), `SearchHit.ownerId` (champ jamais lu), `VaultWrongPassphraseError` + `StorageError` (classes jamais throw) + clé l10n `errorVaultWrongPassphrase`. flutter analyze 0 issue, 75/75 tests verts. |
| **v1.5.3** | 2026-05-13 | UX dashboard : la liste « Prochains rendez-vous » sur la page d'accueil fusionne désormais les rendez-vous (table `appointments`) ET les séances planifiées futures (table `sessions` avec status `planned` ou `confirmed`). Avant : une séance créée pour la semaine prochaine via le shortcut « Nouvelle séance » n'apparaissait pas dans le panneau Accueil — seules les entrées de la table `appointments` étaient regardées. Nouveau `UpcomingEntry` view-model unifié dans `home_stats_providers.dart` ; icône `event_note` pour les séances, `event` pour les RDV. Dédoublonnage automatique entre « Aujourd'hui » et « Prochains » sur le seuil 23h59. Première mise à jour qui **préserve les données** grâce au keystore release stable mis en place en v1.5.2 — plus de wipe `/data/data/<package>/` au passage. |
| **v1.5.2** | 2026-05-13 | (a) Menu kebab `more_vert` à côté du badge « Agenda ✓ » dans la vue Détail séance : deux actions one-tap, « Modifier la séance » (passe par le formulaire complet, qui met à jour l'event Calendar à la sauvegarde) et « Retirer de l'agenda » (efface l'event Calendar + clearCalendarIds sans toucher à la séance). (b) Keystore release stable mis en place — `android/app/health_tech_release.jks` (gitignored) + `android/key.properties`. Tous les builds APK release sont désormais signés par le même certificat : `flutter install` enchaîne en `replace-only` (préservation des données utilisateur entre versions) plutôt que de désinstaller + perdre `/data/data/<package>/` à chaque cycle. Le passage v1.5.1 → v1.5.2 reste destructif **une dernière fois** (debug-cert → release-cert), faire une sauvegarde `.htbk` au préalable. |
| **v1.5.1** | 2026-05-13 | Hotfix sync agenda Android — la v1.5.0 utilisait `CalendarContract.Events.SYNC_DATA1` pour le marqueur anti-collision, mais cette colonne est réservée par Android aux sync adapters (`CALLER_IS_SYNCADAPTER`) ; un app standard se prenait une `IllegalArgumentException` au moment de l'`insert`, et la sync échouait silencieusement. Remplacement par `CUSTOM_APP_PACKAGE` + `CUSTOM_APP_URI`, explicitement prévues pour les apps tierces. Le path "fallback temporel" couvre les events posés en v1.5.0. Aussi : message d'erreur explicite côté formulaire de séance (le `catch on Object` était silencieux). |
| **v1.5.0** | 2026-05-13 | Refactor lourd, 9 axes : (a) **PanicService** complet (wipe vault Keystore + DB SQLCipher + attachments + caches éphémères + restore_staging + image cache + prefs avec whitelist `auto_lock.minutes` seule) accessible dans Réglages avec double confirmation (token « EFFACER » à taper). (b) **DB v5** : nouvelle colonne `filename_encrypted` sur `attachments` (chiffrement au champ FieldCrypto), migration paresseuse des rows v4 à la première lecture, plus jamais de filename client en clair dans la DB. (c) Génération **PDF dans un isolate** via `compute()` (élimine le jank ~300 ms en main thread). (d) Recherche clients **FTS5** activé (10-50× plus rapide sur grand jeu) avec probe d'existence et fallback LIKE transparent, **debounce 250 ms** widget-level sur la search bar. (e) **EmptyState** widget unifié + adoption clients / agenda. (f) Markers agenda **colorés par AppointmentStatus** (cancelled/no_show = cs.error, confirmed = cs.tertiary, done = cs.outline). (g) Argon2id calibration **plafond porté de 6 à 10 itérations** (durcit le brute-force GPU sur devices récents). (h) **Détection de réutilisation passphrase** vault ↔ backup via fingerprint HMAC-SHA256 en mémoire (jamais persisté) — dialog d'avertissement avant export. (i) **UX agenda promue** : `SwitchListTile` dans `Card` visible juste sous le créneau horaire dans le formulaire de séance (helper text dynamique : on / off / déjà-lié / sera-retiré), et bouton « Ajouter à l'agenda » one-tap dans la vue détail quand non synchronisée. |
| **v1.4.6** | 2026-05-13 | Hardening audit 4 axes : (a) anti-fuite latérale Calendar — titre générique par défaut pour `bridge.push(appointment)`, marqueur `SYNC_DATA1=healthtech:<ownerId>` anti-collision data-loss inter-événement, fallback re-create si update touche 0 rows ; (b) cap dur bundle `.htbk` à l'import (256 MiB envelope + 384 MiB cumulé décompressé, anti zip-bomb) ; (c) `pBytes` Argon2id wipé en `finally` même si le worker throw ; (d) plus de dépendances `pointycastle` / `crypto` (non utilisées) ; (e) symétrie session ↔ appointment sur la case « Ajouter à l'agenda » (uncheck = retire l'event Calendar + efface IDs DB). Refresh timezone à chaque push (anti clock-skew voyage). |
| **v1.4.5** | 2026-05-12 | Synchronisation agenda automatique à la sauvegarde d'une séance (DB v4). |
| **v1.4.3** | 2026-05-10 | Page « À propos » (version dynamique via PackageInfo). |
| **v1.4.2** | 2026-05-10 | Biométrie : suppression de la fenêtre 1h (alignement Bitwarden/Aegis). Cold-start et anti-rollback clock conservés. |
| **v1.4.1** | 2026-05-10 | UX : dialog « Import en cours » sur attachments, dropdown animal toujours visible (3 états). |
| **v1.4.0** | 2026-05-09 | Audit zéro-vuln pass : `EphemeralCache`, `SensitiveTextField` anti-Gboard, retry counter sauvegarde, tz refresh, Argon2id passphrase Uint8List wipable, `PaintingBinding.imageCache.clear` au lock. |
| **v1.3.x** | 2026-05-09 | Hybride passphrase / biométrie (Option D 1Password / Bitwarden), mode strict, AAD restore, ordre `_commitStaging` attachments → DB. |
| **v1.2.x** | 2026-05-09 | Lockout passphrase backoff, grace 2 min `onPause`/`onHide` (correction régression écran noir picker), Stopwatch monotonique. |
| **v1.1.x** | 2026-05-09 | KeystoreException distinguée (anti data-loss OTA Samsung), guard `_idFor` notification, `cancelAuthentication` Kotlin avant `deleteKey`. |
| **v1.0.0** | 2026-05-08 | Mise en production : 13 audit-fixes, 19 tests, 3 refactors archi, identité éditeur, hybride pass/bio. |

## Signaler une faille

Contact : security@files-tech.com (PGP à venir).
